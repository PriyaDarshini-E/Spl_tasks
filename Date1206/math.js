export const add =(a,b)=>{
    let c = a+b;
    console.log(`the addition of two number ${a} and ${b} is ${c}`)
}
export const sub =(a,b)=>{
    let c = a-b;
    console.log(`the Subraction of two number ${a} and ${b} is ${c}`)
}